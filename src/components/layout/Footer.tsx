import styled from '@emotion/styled';

const Footer = () => {
  return <FooterLayout>푸터</FooterLayout>;
};

const FooterLayout = styled.footer`
  min-width: 360px;
  width: auto;
  height: 60px;
  background-color: green;
`;

export default Footer;
